name: Deploy alerts in dev
on:
  push:
    branches:
      - 'master'

jobs:
  deploy-dev-sbs:
    name: Deploy to dev-sbs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-sbs
          RESOURCE: dev-alerts.yml
